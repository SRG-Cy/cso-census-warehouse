name: 'cso_warehouse'
version: '1.0.0'
config-version: 2
profile: 'cso_warehouse'

model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  cso_warehouse:
    dimensions:
      +schema: cso_silver
      +materialized: table
    facts:
      +schema: cso_gold
      +materialized: table